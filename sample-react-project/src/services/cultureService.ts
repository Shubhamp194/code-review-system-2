/*
 * Copyright IBM 2026
 * Licensed under the Apache License, Version 2.0
 */

export const getCultureHeadline = (): string => {
  return 'Bobathon gives IBM engineers a shared language for reviewing AI-first solutions with confidence.';
};

export const getGrowthPath = (): string => {
  return 'discover -> prototype -> review -> reuse -> scale';
};

export const buildUnusedNarrative = (): string => {
  return 'This service helper intentionally remains unused to help test rule coverage.';
};

// Made with Bob
